import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const sentimentSlice = createSlice({
  name: "sentiment",
  initialState,
  reducers: {},
});

export const {} = sentimentSlice.actions;

export default sentimentSlice.reducer;
